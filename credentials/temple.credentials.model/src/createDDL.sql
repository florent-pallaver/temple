CREATE TABLE user_identities (
	LOGIN VARCHAR(64) NOT NULL PRIMARY KEY,
	PASS VARCHAR(88) NOT NULL,
	SALT VARCHAR(128) NOT NULL,
	USER_ID INT UNSIGNED NOT NULL,
	UNIQUE U_user_identities_USER_ID (USER_ID)
) ENGINE = MyISAM;

